name: Create a comment on new issue

on: 
  issues:
    types: [opened]
jobs: 
  comment-with-action:
    runs-on: ubuntu-latest
# we will use a community action to comment on a issue
    steps:
      - name: "dump github context"
        run: echo '${{ toJSON(github.event) }}' | jq
        shell: bash